{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Python: Test",
      "type": "debugpy",
      "request": "launch",
      "module": "pytest",
      "args": [
        "-s",  // Pass -s to disable output capturing.
        "${workspaceFolder}/tests"
      ],
      "justMyCode": false
    }
  ]
}
